#include <stdio.h>
int main()
{
    char word;
    int i, len = 0, lenn;
    printf("Enter the String: ");
    scanf("%s", &word);
    word[len] = lenn;
    for (i = 0; i <= word[lenn]; i++)
    {
        len++;
    }
    printf("The length is %d", len);
}